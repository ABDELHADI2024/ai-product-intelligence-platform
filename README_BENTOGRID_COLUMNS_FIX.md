# BentoGrid Columns Fix

This fixes the Vercel TypeScript error where ProductGrid passes `columns={columns}` to BentoGrid, but BentoGrid did not accept a `columns` prop.

Upload only:
- `components/BentoGrid.tsx`

Commit message:
`Fix BentoGrid columns prop`
