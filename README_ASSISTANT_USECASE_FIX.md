# Assistant UseCase Fix

This fixes the TypeScript error in `app/assistant/page.tsx` where priority IDs were inferred as normal strings instead of the strict `UseCase` type.

Upload `app/assistant/page.tsx` to GitHub.
